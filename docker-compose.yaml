# Generated by Okty.io
version: '3'
services:
    php-fpm:
        build: docker/php/
        volumes:
            - './:/usr/share/nginx/html'
    nginx:
        build: docker/nginx/
        ports:
            - '8080:80'
        volumes:
            - './:/usr/share/nginx/html'
    composer:
        image: 'composer:latest'
        volumes:
            - './:/app'
        command: "install"
    phpunit:
        build: docker/phpunit
        volumes:
            - './:/app'